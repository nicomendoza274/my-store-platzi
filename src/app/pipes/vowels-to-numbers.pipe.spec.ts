import { VowelsToNumbersPipe } from './vowels-to-numbers.pipe';

describe('VowelsToNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new VowelsToNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
